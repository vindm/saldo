# `policies` folder

Instructions, rules, checklists, and workflows for running the bookkeeping practice.

## IMPORTANT

The `secrets/` folder is never shared externally. If we ever sync the working
folder into git or the cloud, `secrets/` must be excluded first.

See also `secrets/README.md`.
